name: Format Pre-Alpha Stripe Sandbox

on:
  push:
    branches:
      - test/pre-alpha-stripe-sandbox

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: test/pre-alpha-stripe-sandbox

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.15.0 --activate

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Format Stripe adapter changes
        run: >-
          pnpm exec prettier --write
          apps/api/src/billing/stripe-payment-gateway.ts
          tests/integration/pre-alpha-stripe-sandbox.test.mjs

      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/api/src/billing/stripe-payment-gateway.ts tests/integration/pre-alpha-stripe-sandbox.test.mjs
          git commit -m "style: format Stripe sandbox adapter changes"
          git push origin HEAD:test/pre-alpha-stripe-sandbox
