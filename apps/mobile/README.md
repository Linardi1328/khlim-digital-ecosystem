# Mobile workspace

`apps/mobile` is intentionally reserved for the later KHLIM Super App.

The current production priority is the customer website/member portal, admin web application, shared API, membership infrastructure and payment foundation. Do not build mobile-only business logic here. When native mobile development is activated, this client must consume the same authenticated REST APIs and domain rules used by the website and admin platform.
